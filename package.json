{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish:versions:script": "sh ./scripts/release.sh",
    "publish:versions:manual": "yarn lerna version"
  },
  "devDependencies": {
    "lerna": "^9.0.7",
    "prettier": "^3.8.0"
  }
}
